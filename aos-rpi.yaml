desc: "AosCore build for Raspberry Pi 5"
min_ver: "0.24"

variables:
  # Build configuration
  YOCTOS_WORK_DIR: "yocto"
  DOM0_OS: "zephyr"

  INSTALL_BUILD_DIR: "build-install"
  DOMD_BUILD_DIR: "build-domd-%{DOMD_NODE_TYPE}"
  DOM0_BUILD_DIR: "zephyr/build-dom0"

  XT_XEN_DTBO_NAME: "%{SOC_FAMILY}-%{MACHINE}-xen.dtbo"
  XT_DOMD_DTB_NAME: "%{SOC_FAMILY}-%{MACHINE}-domd.dtb"
  XT_XEN_DTB_NAME: "%{SOC_FAMILY}-%{MACHINE}-xen.dtb"

  # Addresses for u-boot

  DOMD_IMG_ADDR: "0x3500000"
  DOMD_DTB_ADDR: "0xb000000"
  XEN_IMG_ADDR: "0xb100000"
  XEN_DTBO_ADDR: "0xb300000"
  XENPOLICY_IMG_ADDR: "0xb400000"

  # SELinux config

  SELINUX_BOOT_ARGS: ""

  # Aos config

  # Aos unit configuration
  AOS_UNIT_MODEL: "aos-ros-rpi"
  AOS_UNIT_VERSION: "1.0.0"

  AOS_COMPONENT_RUNTIME_PREFIX: "%{AOS_UNIT_MODEL}-%{AOS_UNIT_VERSION}-%{DOMD_NODE_TYPE}-%{MACHINE}-%{DOMD_ROOT}-"

  # Version configuration

  AOS_BUNDLE_IMAGE_VERSION: "1.1.1"
  AOS_ROOTFS_REF_VERSION: "1.1.0"

  # Aos node configuration
  MAIN_NODE_HOSTNAME: "main"

  # Network configuration
  MAIN_NODE_IP: "10.0.0.100"

  AOS_BASE_IMAGE: "aos-image-rpi"
  AOS_INSTALL_IMAGE: "core-image-minimal"

  # Node info
  AOS_ARCHITECTURE: "arm64"
  AOS_ARCHITECTURE_VARIANT: ""
  AOS_OS: "linux"
  AOS_OS_VERSION: ""

common_data:
  common_yocto_sources: &COMMON_YOCTO_SOURCES
    - type: git
      url: "https://git.yoctoproject.org/poky"
      rev: "scarthgap"

    - type: git
      url: "https://git.openembedded.org/meta-openembedded"
      rev: "scarthgap"

    - type: git
      url: "https://git.yoctoproject.org/meta-virtualization"
      rev: "scarthgap"

    - type: git
      url: "https://git.yoctoproject.org/meta-security"
      rev: "scarthgap"

    - type: git
      url: "https://git.yoctoproject.org/meta-selinux"
      rev: "scarthgap"

    - type: git
      url: "https://github.com/xen-troops/meta-xt-common.git"
      rev: "v0.3.0"

    - type: git
      url: "https://github.com/kraj/meta-clang.git"
      rev: "scarthgap"

    - type: git
      url: "https://github.com/ros/meta-ros.git"
      rev: "scarthgap"

    - type: git
      url: "https://github.com/zboszor/meta-clang-revival"
      rev: "scarthgap"

    - type: git
      url: "https://github.com/zboszor/meta-python-ai"
      rev: "scarthgap"

    - type: git
      url: "https://github.com/robwoolley/meta-intel-realsense.git"
      rev: "scarthgap"

    - type: git
      url: "https://github.com/Jarsop/meta-zenoh.git"
      rev: "scarthgap"

    - type: git
      url: "https://github.com/aosedge/meta-aos"
      rev: "develop"

    - type: git
      url: "https://github.com/aosedge/meta-aos-rpi.git"
      rev: "demo_ros"

  common_conf: &COMMON_CONF
    - [SSTATE_DIR, "${TOPDIR}/../../../common_data/sstate"]
    - [DL_DIR, "${TOPDIR}/../../../common_data/downloads"]

    # Init manager
    - [INIT_MANAGER, "systemd"]

    # Use hypervisor console on all guests
    - [SERIAL_CONSOLES, "115200;hvc0"]

    # Skip warning about missing "virtualization" distro feature
    - [SKIP_META_VIRT_SANITY_CHECK, "1"]

    # Do not install kernel image to rootfs to decrease initrd size
    - ["RDEPENDS_${KERNEL_PACKAGE_NAME}-base", ""]

    # Distro features

    # Remove features that we are not using
    - [
        DISTRO_FEATURES:remove,
        "x11 gtk gobject-introspection-data nfc irda zeroconf 3g sysvinit
        acl alsa argp pcmcia usbgadget opengl ptest multiarch wayland
        vulkan sysvinit",
      ]
    # Remove ptest to reduce the build time
    - [DISTRO_FEATURES:remove, "ptest"]

    # Enable security features
    - [DISTRO_FEATURES:append, " security"]

      # Omit unneeded ROS recipes from the build
    - [
        BBMASK:append,
        " generated-recipes/libcamera/libcamera_
        generated-recipes/raph-robot/
        generated-recipes/parameter-expression/
        recipes-core/packagegroups/packagegroup-ros-world-jazzy.bb
        recipes-core/packagegroups/packagegroup-ros-world\\.bb
        recipes-core/images/ros-image-world.bb
        generated-recipes/navigation2/
        recipes-bbappends/navigation2/
        recipes-bbappends-nav2/
        generated-recipes/trac-ik/
        generated-recipes/ardrone-ros/
        generated-recipes/easynav-plugins/
        generated-recipes/turtlebot4/turtlebot4-navigation_
        generated-recipes/turtlebot4-simulator/
        generated-recipes/turtlebot3-home-service-challenge/
        generated-recipes/turtlebot3-manipulation/turtlebot3-manipulation-navigation2_
        generated-recipes/neo-nav2-bringup/
        generated-recipes/ai-worker/ffw-navigation_
        ",
      ]

    # Set ROS2 Jazzy configuration
    - [PACKAGECONFIG:append:pn-clang, " libomp"]
    - [FORTRAN:forcevariable, ",fortran"]
    - [RUNTIMETARGET:append:pn-gcc-runtime, " libquadmath"]
    - [HOSTTOOLS:append, " gfortran"]
    - [ZENOH_SHARED_MEMORY, "true"]
    - [ZENOH_UNSTABLE_API, "true"]
    - [INSANE_SKIP:pn-zenoh-c, "buildpaths"]
    - [DEBUG_PREFIX_MAP:remove:pn-zenoh-c, "-fcanon-prefix-map"]
    - [DEBUG_PREFIX_MAP:remove:pn-zenoh-c-native, "-fcanon-prefix-map"]

    - [ROS_DISTRO, "jazzy"]
    - [
        ROS_WORLD_SKIP_GROUPS:append,
        " moveit
        fortran
        qt5
        ogre
        webots-python-modules
        opengl
        x11
        gazebo
        gazebo11
        hunter",
      ]

    - [PREFERRED_VERSION_python3-boto3, "1.34.131"]
    - [PREFERRED_VERSION_python3-botocore, "1.34.131"]

    - [LICENSE_FLAGS_ACCEPTED:append, " commercial synaptics-killswitch"]

  domd-common-layers: &DOMD_COMMON_LAYERS
    - "../meta-virtualization"
    - "../meta-openembedded/meta-oe"
    - "../meta-openembedded/meta-initramfs"
    - "../meta-openembedded/meta-networking"
    - "../meta-openembedded/meta-perl"
    - "../meta-openembedded/meta-python"
    - "../meta-openembedded/meta-webserver"
    - "../meta-zenoh/meta-zenoh"
    - "../meta-ros/meta-ros-common"
    - "../meta-ros/meta-ros2"
    - "../meta-ros/meta-ros2-jazzy"
    - "../meta-clang-revival"
    - "../meta-python-ai"
    - "../meta-clang"
    - "../meta-openembedded/meta-filesystems"
    - "../meta-security"
    - "../meta-selinux"
    - "../meta-intel-realsense"
    - "../meta-xt-common/meta-xt-domx"
    - "../meta-xt-common/meta-xt-driver-domain"
    - "../meta-xt-common/meta-xt-security"
    - "../meta-raspberrypi"
    - "../meta-xt-rpi5"
    - "../meta-aos"
    - "../meta-aos-rpi/meta-aos-rpi-domd-common"

  common_dom_conf: &COMMON_DOM_CONF # Make logs persistent
    - [VOLATILE_LOG_DIR, "no"]

    # Generate ext4 image files
    - [IMAGE_FSTYPES:append, " ext4"]

    # Initramfs configuration
    - [INITRAMFS_IMAGE, "aos-image-initramfs"]
    - [INITRAMFS_IMAGE_BUNDLE, "1"]

    # Unit model
    - [AOS_UNIT_MODEL, "%{AOS_UNIT_MODEL}"]
    - [AOS_UNIT_VERSION, "%{AOS_UNIT_VERSION}"]

    # Image versions
    - [AOS_ROOTFS_IMAGE_VERSION, "%{AOS_BUNDLE_IMAGE_VERSION}"]
    - [AOS_BOOT_IMAGE_VERSION, "%{AOS_BUNDLE_IMAGE_VERSION}"]

    # Requires to properly generate Aos FOTA
    - [IMAGE_BASENAME, "%{AOS_BASE_IMAGE}"]

components:
  dom0:
    default: true
    build-dir: "%{DOM0_BUILD_DIR}"
    sources:
      - type: west
        url: "https://github.com/aosedge/aos_core_zephyr"
        rev: "v1.1.3"

    builder:
      type: zephyr
      board: "%{ZEPHYR_DOM0_MACHINE}"
      target: "aos_core_zephyr"
      work_dir: build
      vars:
        - 'CONFIG_AOS_NODE_TYPE="%{AOS_DOM0_NODE_TYPE}"'
        - 'CONFIG_TA_DEPLOY_DIR="../../../%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/aos/ta"'
        - 'CONFIG_AOS_ROOT_CA_PATH="../../../%{YOCTOS_WORK_DIR}/meta-aos/recipes-support/ca-certificates/ca-certificates/AosRootCA.crt"'

      target_images:
        - "build/zephyr/zephyr.bin"

      additional_deps:
        - "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/aos/ta"

  domd:
    default: true
    build-dir: "%{YOCTOS_WORK_DIR}"
    sources:
      - *COMMON_YOCTO_SOURCES
      - type: git
        url: "https://git.yoctoproject.org/meta-raspberrypi"
        rev: "2c646d29912dcc873469a57b1c207e1549c5094d"

      - type: git
        url: "https://github.com/xen-troops/meta-xt-rpi5.git"
        rev: "e9473cc4e5b1e1d19df7da59daa17936d636ee21"

    builder:
      type: yocto
      work_dir: "%{DOMD_BUILD_DIR}"
      conf:
        - *COMMON_CONF
        - *COMMON_DOM_CONF
        - [DISTRO, "aos-core"]
        - [MACHINE, "%{MACHINE}"]
        - [RPI_SOC_FAMILY, "%{SOC_FAMILY}"]
        - [XT_DOM_NAME, "domd"]
        - [XT_DEVICE_TREES, "%{XT_XEN_DTBO_NAME}"]
        - [DOMD_CAN_TYPE, "%{DOMD_CAN_TYPE}"]
        - [PREFERRED_VERSION_xen, "4.19.0+git"]
        - [PREFERRED_VERSION_xen-tools, "4.19.0+git"]
        - [PREFERRED_VERSION_u-boot, "2024.04"]

        # Addresses for u-boot
        - [DOMD_IMG_ADDR, "%{DOMD_IMG_ADDR}"]
        - [DOMD_DTB_ADDR, "%{DOMD_DTB_ADDR}"]
        - [XEN_IMG_ADDR, "%{XEN_IMG_ADDR}"]
        - [XEN_DTBO_ADDR, "%{XEN_DTBO_ADDR}"]
        - [XENPOLICY_IMG_ADDR, "%{XENPOLICY_IMG_ADDR}"]
        - [
            DOMD_OVERLAYS:append,
            " %{SOC_FAMILY}-%{MACHINE}-usb.dtbo %{SOC_FAMILY}-%{MACHINE}-can-%{DOMD_CAN_TYPE}.dtbo",
          ]
        - [MACHINE_FEATURES:append, " domd_usb domd_can"]

        # RPI specific
        - [ENABLE_UART, "1"]
        - [RPI_USE_U_BOOT, "1"]
        - [PREFERRED_PROVIDER_u-boot-default-script, "xt-rpi-u-boot-scr"]
        - [UART_BOORLOADER, "1"]
        - [KERNEL_IMAGETYPE_UBOOT, "Image"]
        - [DISTRO_FEATURES:append, " xen"]
        - [
            DOMD_BOOTARGS,
            "console=ttyAMA0 earlycon=xen earlyprintk=xen clk_ignore_unused
            root=\\\\/dev\\\\/%{DOMD_BLK_DEV}1 rootfstype=ext4 rootwait quiet ro rootwait pci=pcie_bus_perf
            cgroup_no_v1=all systemd.unified_cgroup_hierarchy=1 cgroup_enable=memory
            ${@bb.utils.contains('DISTRO_FEATURES', 'selinux', '%{SELINUX_BOOT_ARGS}', '', d)}
            vardir.disk=\\\\/dev\\\\/%{DOMD_BLK_DEV}2
            opendisk.target=\\\\/dev\\\\/%{DOMD_BLK_DEV}3
            opendisk.pkcs11=optee
            aosupdate.disk=\\\\/dev\\\\/aosvg\\\\/workdirs
            aosupdate.path=sm\\\\/runtimes\\\\/rootfs
            ${@bb.utils.contains('DISTRO_FEATURES', 'selinux', 'aosupdate.selinux_module=\\\\/usr\\\\/share\\\\/selinux\\\\/aos\\\\/base.pp', '', d)}",
          ]
        - [AOS_COMPONENT_RUNTIME_PREFIX, "%{AOS_COMPONENT_RUNTIME_PREFIX}"]
        - [AOS_NODE_TYPE, "%{AOS_DOMD_NODE_TYPE}"]
        - [AOS_DNS_IP, "%{MAIN_NODE_IP}"]

        - [AOS_ARCHITECTURE, "%{AOS_ARCHITECTURE}"]
        - [AOS_ARCHITECTURE_VARIANT, "%{AOS_ARCHITECTURE_VARIANT}"]
        - [AOS_OS, "%{AOS_OS}"]
        - [AOS_OS_VERSION, "%{AOS_OS_VERSION}"]

      build_target: "%{AOS_BASE_IMAGE}"
      layers:
        - *DOMD_COMMON_LAYERS

      target_images:
        - "tmp/deploy/images/%{MACHINE}/bootfiles"

        - "tmp/deploy/images/%{MACHINE}/armstub8-2712.bin"

        - "tmp/deploy/images/%{MACHINE}/Image.gz-initramfs-%{MACHINE}.bin"
        - "tmp/deploy/images/%{MACHINE}/xen-%{MACHINE}"
        - "tmp/deploy/images/%{MACHINE}/xenpolicy-%{MACHINE}"
        - "tmp/deploy/images/%{MACHINE}/u-boot-%{MACHINE}.bin"
        - "tmp/deploy/images/%{MACHINE}/boot.scr"

        - "tmp/deploy/images/%{MACHINE}/%{XT_XEN_DTBO_NAME}"
        - "tmp/deploy/images/%{MACHINE}/%{XT_DOMD_DTB_NAME}"

        - "tmp/deploy/images/%{MACHINE}/bcm2712-rpi-5-b.dtb"

        - "tmp/deploy/images/%{MACHINE}/mmc-passthrough.dtbo"
        - "tmp/deploy/images/%{MACHINE}/usb-passthrough.dtbo"
        - "tmp/deploy/images/%{MACHINE}/pcie1-passthrough.dtbo"
        - "tmp/deploy/images/%{MACHINE}/%{SOC_FAMILY}-%{MACHINE}-mmc.dtbo"
        - "tmp/deploy/images/%{MACHINE}/%{SOC_FAMILY}-%{MACHINE}-usb.dtbo"
        - "tmp/deploy/images/%{MACHINE}/%{SOC_FAMILY}-%{MACHINE}-pcie1.dtbo"
        - "tmp/deploy/images/%{MACHINE}/%{SOC_FAMILY}-%{MACHINE}-can-%{DOMD_CAN_TYPE}.dtbo"

        - "tmp/deploy/images/%{MACHINE}/%{AOS_BASE_IMAGE}-%{MACHINE}.rootfs.ext4"
        - "tmp/deploy/images/%{MACHINE}/%{AOS_BASE_IMAGE}-%{MACHINE}-var.ext4"

        - "tmp/deploy/images/%{MACHINE}/aos/boot/version"
        - "tmp/deploy/images/%{MACHINE}/aos/ta"

  aos-sdk:
    builder:
      type: custom_script
      work_dir: "workdir"
      script: "../yocto/meta-aos/scripts/sdk_builder.py"
      target_images:
        - "../output/sdk"

      additional_deps:
        - "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/%{AOS_BASE_IMAGE}-%{MACHINE}.rootfs.ext4"

      sdk:
        yocto_dir: "../%{YOCTOS_WORK_DIR}"
        build_dir: "%{DOMD_BUILD_DIR}"
        output_dir: "../output/sdk"
        base_image: "%{AOS_BASE_IMAGE}"

  install:
    default: true
    build-dir: "%{YOCTOS_WORK_DIR}"
    sources:
      - *COMMON_YOCTO_SOURCES
      - type: git
        url: "https://git.yoctoproject.org/meta-raspberrypi"
        rev: "2c646d29912dcc873469a57b1c207e1549c5094d"

    builder:
      type: yocto
      work_dir: "%{INSTALL_BUILD_DIR}"
      conf:
        - *COMMON_CONF
        - [MACHINE, "%{MACHINE}"]
        - [CMDLINE_AOS_DISK, "%{DOMD_BLK_DEV}"]
        - [LICENSE_FLAGS_ACCEPTED, "synaptics-killswitch"]

        # Generate ext4 image files rpi-sdimg
        - [IMAGE_FSTYPES, "rpi-sdimg"]
        - [SDIMG_VFAT_DEPLOY, "1"]
        - [SDIMG_ROOTFS_TYPE, "ext4"]
        - [BOOT_SPACE, "262144"]
        - [
            RPI_SDIMG_EXTRA_DEPENDS,
            "initramfs-aos-install:do_install rpi-cmdline:do_deploy rpi-config:do_deploy",
          ]

        # Initramfs configuration
        - [INITRAMFS_IMAGE, "aos-install-initramfs"]
        - [INITRAMFS_IMAGE_BUNDLE, "1"]
        - [INITRAMFS_MAXSIZE, "524288"]

        # RPI specific
        - [ENABLE_UART, "1"]

      build_target: "%{AOS_INSTALL_IMAGE}"
      layers:
        - "../meta-openembedded/meta-oe"
        - "../meta-openembedded/meta-networking"
        - "../meta-openembedded/meta-perl"
        - "../meta-openembedded/meta-python"
        - "../meta-raspberrypi"
        - "../meta-aos-rpi/meta-aos-rpi-install"

      target_images:
        - "tmp/deploy/images/%{MACHINE}/%{AOS_INSTALL_IMAGE}-%{MACHINE}.rootfs.vfat"

  install_archive:
    build-dir: "%{INSTALL_BUILD_DIR}"
    builder:
      type: custom_script
      args: "%{DOMD_ROOT}"
      script: "../scripts/image_archive.sh"

      additional_deps:
        - "boot-%{DOMD_NODE_TYPE}-%{DOMD_ROOT}.img"
        - "rootfs-%{DOMD_NODE_TYPE}-%{DOMD_ROOT}.img"

      target_images:
        - "../boot-%{DOMD_NODE_TYPE}-%{DOMD_ROOT}.img.gz"
        - "../rootfs-%{DOMD_NODE_TYPE}-%{DOMD_ROOT}.img.gz"

  layers:
    builder:
      type: custom_script
      work_dir: "workdir"
      script: "../yocto/meta-aos/scripts/layer_builder.py"
      target_images:
        - "../output/layers/config.yaml"
        - "../output/layers/kuksa-client/%{AOS_OS}-%{AOS_ARCHITECTURE}-1.0.0.tar.gz"
        - "../output/layers/pylibs/%{AOS_OS}-%{AOS_ARCHITECTURE}-1.0.0.tar.gz"
        - "../output/layers/rpilibs/%{AOS_OS}-%{AOS_ARCHITECTURE}-1.0.0.tar.gz"

      additional_deps:
        - "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/%{AOS_BASE_IMAGE}-%{MACHINE}.rootfs.ext4"

      layers:
        architecture: "%{AOS_ARCHITECTURE}"
        os: "%{AOS_OS}"
        output_dir: "../output/layers"
        publisher:
          company: "EPAM Systems"

        publish:
          tlsKey: "aos-user-oem.p12"

        yocto_dir: "../%{YOCTOS_WORK_DIR}"
        build_dir: "%{DOMD_BUILD_DIR}"
        base_image: "%{AOS_BASE_IMAGE}"
        items:
          kuksa-client:
            target: "aos-kuksa-client-layer"
            version: "%{AOS_BUNDLE_IMAGE_VERSION}"

          pylibs:
            target: "aos-pylibs-layer"
            version: "%{AOS_BUNDLE_IMAGE_VERSION}"

          rpilibs:
            target: "aos-rpilibs-layer"
            version: "%{AOS_BUNDLE_IMAGE_VERSION}"

          boto3:
            target: "aos-boto3-layer"
            version: "%{AOS_BUNDLE_IMAGE_VERSION}"

          ml:
            target: "aos-ml-layer"
            version: "%{AOS_BUNDLE_IMAGE_VERSION}"

          mqtt:
            target: "aos-mqtt-layer"
            version: "%{AOS_BUNDLE_IMAGE_VERSION}"

          rosbag:
            target: "aos-rosbag-layer"
            version: "%{AOS_BUNDLE_IMAGE_VERSION}"

          valkey:
            target: "aos-valkey-layer"
            version: "%{AOS_BUNDLE_IMAGE_VERSION}"

  fota:
    builder:
      type: custom_script
      work_dir: "workdir"
      script: "../yocto/meta-aos/scripts/fota_builder.py"
      target_images:
        - "../output/fota"

      additional_deps:
        - "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/%{AOS_BASE_IMAGE}-%{MACHINE}.rootfs.ext4"

      components:
        architecture: "%{AOS_ARCHITECTURE}"
        os: "%{AOS_OS}"
        output_dir: "../output/fota"
        publisher:
          company: "EPAM Systems"

        publish:
          tlsKey: "aos-user-oem.p12"

        work_dir: "workdir"
        items:
          rootfs-full:
            componentType: "%{AOS_COMPONENT_RUNTIME_PREFIX}rootfs"
            mediaType: "application/vnd.aos.image.component.full.v1+squashfs"
            enabled: true
            method: "overlay"
            ostree_repo: "../../ostree_repo/%{AOS_DOMD_NODE_TYPE}-rootfs"
            yocto_dir: "../%{YOCTOS_WORK_DIR}"
            build_dir: "%{DOMD_BUILD_DIR}"
            type: "full"
            title: "Aos RPI %{AOS_DOMD_NODE_TYPE} rootfs full image"
            description: "Aos RPI %{AOS_DOMD_NODE_TYPE} rootfs full image for ${MACHINE}"
            version: "%{AOS_BUNDLE_IMAGE_VERSION}"
            fileName: "%{AOS_DOMD_NODE_TYPE}-rootfs-full-%{AOS_BUNDLE_IMAGE_VERSION}.squashfs"
            exclude:
              - "home/*"
              - "var/*"

          rootfs-incremental:
            componentType: "%{AOS_COMPONENT_RUNTIME_PREFIX}rootfs"
            mediaType: "application/vnd.aos.image.component.inc.v1+squashfs"
            enabled: false
            method: "overlay"
            ostree_repo: "../../ostree_repo/%{AOS_DOMD_NODE_TYPE}-rootfs"
            yocto_dir: "../%{YOCTOS_WORK_DIR}"
            build_dir: "%{DOMD_BUILD_DIR}"
            type: "incremental"
            title: "Aos RPI %{AOS_DOMD_NODE_TYPE} rootfs incremental image"
            description: "Aos RPI %{AOS_DOMD_NODE_TYPE} rootfs incremental image for ${MACHINE}"
            version: "%{AOS_BUNDLE_IMAGE_VERSION}"
            requiredVersion: "%{AOS_ROOTFS_REF_VERSION}"
            fileName: "%{AOS_DOMD_NODE_TYPE}-rootfs-incremental-%{AOS_BUNDLE_IMAGE_VERSION}.squashfs"
            exclude:
              - "home/*"
              - "var/*"
images:
  boot-%{DOMD_NODE_TYPE}-%{DOMD_ROOT}:
    type: gpt
    desc: "Full RPI image"
    sector_size: 512
    partitions:
      boot:
        gpt_type: ebd0a0a2-b9e5-4433-87c0-68b6b72699c7
        type: vfat
        size: 512 MiB
        items:
          "/": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/bootfiles"

          "armstub8-2712.bin": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/armstub8-2712.bin"

          "Image.gz": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/Image.gz-initramfs-%{MACHINE}.bin"
          "xen": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/xen-%{MACHINE}"
          "xenpolicy": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/xenpolicy-%{MACHINE}"
          "u-boot": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/u-boot-%{MACHINE}.bin"
          "boot.scr": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/boot.scr"

          "%{XT_XEN_DTBO_NAME}": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/%{XT_XEN_DTBO_NAME}"
          "%{XT_DOMD_DTB_NAME}": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/%{XT_DOMD_DTB_NAME}"

          "bcm2712-rpi-5-b.dtb": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/bcm2712-rpi-5-b.dtb"

          "mmc-passthrough.dtbo": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/mmc-passthrough.dtbo"
          "usb-passthrough.dtbo": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/usb-passthrough.dtbo"
          "pcie1-passthrough.dtbo": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/pcie1-passthrough.dtbo"
          "%{SOC_FAMILY}-%{MACHINE}-usb.dtbo": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/%{SOC_FAMILY}-%{MACHINE}-usb.dtbo"
          "%{SOC_FAMILY}-%{MACHINE}-mmc.dtbo": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/%{SOC_FAMILY}-%{MACHINE}-mmc.dtbo"
          "%{SOC_FAMILY}-%{MACHINE}-pcie1.dtbo": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/%{SOC_FAMILY}-%{MACHINE}-pcie1.dtbo"
          "%{SOC_FAMILY}-%{MACHINE}-can-%{DOMD_CAN_TYPE}.dtbo": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/%{SOC_FAMILY}-%{MACHINE}-can-%{DOMD_CAN_TYPE}.dtbo"

          "zephyr.bin": "%{DOM0_BUILD_DIR}/build/zephyr/zephyr.bin"

          "aos/version": "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/aos/boot/version"

      dom0:
        gpt_type: ebd0a0a2-b9e5-4433-87c0-68b6b72699c7
        type: vfat
        size: 512 MiB

  rootfs-%{DOMD_NODE_TYPE}-%{DOMD_ROOT}:
    type: gpt
    desc: "Full RPI image"
    sector_size: 512
    partitions:
      domd_rootfs:
        gpt_type: B921B045-1DF0-41C3-AF44-4C6F280D3FAE # Linux aarch64 root
        type: raw_image
        image_path: "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/%{AOS_BASE_IMAGE}-%{MACHINE}.rootfs.ext4"

      domd_var:
        gpt_type: B921B045-1DF0-41C3-AF44-4C6F280D3FAE # Linux aarch64 root
        type: raw_image
        image_path: "%{YOCTOS_WORK_DIR}/%{DOMD_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/%{AOS_BASE_IMAGE}-%{MACHINE}-var.ext4"

      domd_aos:
        gpt_type: CA7D7CCB-63ED-4C53-861C-1742536059CC # LUKS partition
        type: empty
        size: 32768 MiB

  install-%{DOMD_NODE_TYPE}-%{DOMD_ROOT}:
    type: gpt
    desc: "Full RPI image"
    sector_size: 512
    partitions:
      boot:
        gpt_type: C12A7328-F81F-11D2-BA4B-00A0C93EC93B
        type: raw_image
        sparse: false
        image_path: "%{YOCTOS_WORK_DIR}/%{INSTALL_BUILD_DIR}/tmp/deploy/images/%{MACHINE}/%{AOS_INSTALL_IMAGE}-%{MACHINE}.rootfs.vfat"

      install:
        gpt_type: ebd0a0a2-b9e5-4433-87c0-68b6b72699c7
        type: ext4
        size: 700 MiB
        items:
          "boot.img.gz": "boot-%{DOMD_NODE_TYPE}-%{DOMD_ROOT}.img.gz"
          "rootfs.img.gz": "rootfs-%{DOMD_NODE_TYPE}-%{DOMD_ROOT}.img.gz"

parameters:
  DOMD_NODE_TYPE:
    desc: "Domd node type to build"
    "single":
      default: true
      overrides:
        variables:
          AOS_DOM0_NODE_TYPE: "%{AOS_UNIT_MODEL}-single-dom0"
          AOS_DOMD_NODE_TYPE: "%{AOS_UNIT_MODEL}-single-domd"
          DOMD_NODE_TYPE: "single"

        components:
          domd:
            builder:
              conf:
                - [AOS_MAIN_NODE, "1"]
                - [AOS_NODE_IP, "%{MAIN_NODE_IP}/24"]
                - [AOS_USE_DHCP, "1"]

              layers:
                - "../meta-aos-rpi/meta-aos-rpi-domd-main"

    "main":
      overrides:
        variables:
          AOS_DOM0_NODE_TYPE: "%{AOS_UNIT_MODEL}-main-dom0"
          AOS_DOMD_NODE_TYPE: "%{AOS_UNIT_MODEL}-main-domd"
          DOMD_NODE_TYPE: "main"

        components:
          domd:
            builder:
              conf:
                - [AOS_MAIN_NODE, "1"]
                - [AOS_NODE_IP, "%{MAIN_NODE_IP}/24"]
                - [AOS_NODE_GW_IP, "10.0.0.1"]
                - [AOS_USE_DHCP, "0"]
                - [AOS_HOSTS:append, " %{MAIN_NODE_IP}=%{MAIN_NODE_HOSTNAME}"]

              layers:
                - "../meta-aos-rpi/meta-aos-rpi-domd-main"

    "secondary":
      overrides:
        variables:
          AOS_DOM0_NODE_TYPE: "%{AOS_UNIT_MODEL}-secondary-dom0"
          AOS_DOMD_NODE_TYPE: "%{AOS_UNIT_MODEL}-secondary-domd"
          DOMD_NODE_TYPE: "secondary"

        components:
          domd:
            builder:
              conf:
                - [AOS_MAIN_NODE, "0"]
                - [AOS_USE_DHCP, "1"]

              layers:
                - "../meta-aos-rpi/meta-aos-rpi-domd-node"

  DOMD_CAN_TYPE:
    desc: "Domd CAN chip type"
    "SEEED-FD":
      default: true
      overrides:
        variables:
          DOMD_CAN_TYPE: "seeed-fd"

    "MCP2515":
      overrides:
        variables:
          DOMD_CAN_TYPE: "mcp2515"

  MACHINE:
    desc: "Raspberry Pi machine"
    rpi5:
      default: true
      overrides:
        variables:
          MACHINE: "raspberrypi5"
          SOC_FAMILY: "bcm2712"
          ZEPHYR_DOM0_MACHINE: "rpi_5"

  CACHE_LOCATION:
    desc: "Indicated where cache and downloads are stored: inside build dir or outside."
    outside:
      default: true

    inside:
      overrides:
        common_data:
          common_conf:
            - [SSTATE_DIR, "${TOPDIR}/../../common_data/sstate"]
            - [DL_DIR, "${TOPDIR}/../../common_data/downloads"]

  DEBUG_TWEAKS:
    desc: "Allow configure build with debug-tweaks."
    disabled:
      default: true
      overrides:
        common_data:
          common_conf:
            - [EXTRA_IMAGE_FEATURES:remove, " debug-tweaks"]

    enabled:
      default: false

  DOMD_ROOT:
    desc: "Domd root device"
    usb:
      default: true
      overrides:
        variables:
          DOMD_ROOT: usb
          DOMD_BLK_DEV: "sda"

    nvme:
      overrides:
        variables:
          DOMD_ROOT: nvme
          DOMD_BLK_DEV: "nvme0n1p"

        components:
          domd:
            builder:
              conf:
                - [DOMD_OVERLAYS, "%{SOC_FAMILY}-%{MACHINE}-pcie1.dtbo"]
                - [MACHINE_FEATURES:append, " domd_nvme"]

  SELINUX:
    desc: "Enables SELinux"
    enabled:
      overrides:
        variables:
          SELINUX_BOOT_ARGS: "security=selinux"

        common_data:
          common_domd_conf:
            - [DISTRO_FEATURES:append, " acl xattr pam selinux"]
            - [FIRST_BOOT_RELABEL, "1"]
            - [PREFERRED_PROVIDER_virtual/refpolicy, "refpolicy-aos"]
            - [
                "SKIP_RECIPE[uutils-coreutils]",
                "it caused build issue and we don't need it",
              ]

    permissive:
      overrides:
        variables:
          SELINUX_BOOT_ARGS: "security=selinux enforcing=0"

        common_data:
          common_domd_conf:
            - [DISTRO_FEATURES:append, " acl xattr pam selinux"]
            - [FIRST_BOOT_RELABEL, "1"]
            - [PREFERRED_PROVIDER_virtual/refpolicy, "refpolicy-aos"]
            - [
                "SKIP_RECIPE[uutils-coreutils]",
                "it caused build issue and we don't need it",
              ]

    disabled:
      default: true

  ENABLE_AOS_IN_DOM0:
    desc: "Enable Aos in Dom0"
    "yes":
      overrides:
        components:
          aos-vm:
            builder:
              conf:
                - [
                    IMAGE_INSTALL:append,
                    " ${@'aos-messageproxy' if bb.utils.to_boolean(d.getVar('AOS_MAIN_NODE')) else ''}",
                  ]

    "no":
      default: true
      overrides:
        components:
          dom0:
            builder:
              vars:
                - "CONFIG_AOS_ENABLE=n"
